#
# https://stackoverflow.com/questions/58682693/how-can-i-create-bubble-grid-charts-in-ggplot
#
# bubble_grid <- ggplot(data = dt, mapping = aes(x = sex, y = factor(year), size = bill_length_mm)) + geom_point()
